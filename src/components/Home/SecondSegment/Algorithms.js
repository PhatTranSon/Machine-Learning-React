import AlgorithmCarousel from './AlgorithmCarousel';

const Algorithm = (props) => {
    return (
        <div style={{padding: "20px"}} className="carousel-algo">
            <h1 className="carousel-title">Explore machine learning</h1>
            <AlgorithmCarousel/>
        </div>
    )
}

export default Algorithm;